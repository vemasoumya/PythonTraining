from setuptools import setup, find_packages

setup(
    name="commonpackage",
    version="0.1",
    install_requires=[],
    setup_requires=['wheel'],
    data_files=[],
    packages=find_packages()
)


